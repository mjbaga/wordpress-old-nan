<?php
/*
 * Site Prefix
 */
if( !defined( 'ADELPHI_SITE_PREFIX' ) ) {
  define( 'ADELPHI_SITE_PREFIX', 'ad_' );
}

/*
 * Plugin Directory
 */
if( !defined( 'LP_BREADCRUMBS_PLUGIN_DIR' ) ) {
  define( 'LP_BREADCRUMBS_PLUGIN_DIR', dirname( __FILE__ ) . '/..' );
}

/*
 * Breadcrumbs Options name in cms settings
 */
if( !defined( 'LP_BREADCRUMBS_OPTIONS_NAME' ) ) {
  define( 'LP_BREADCRUMBS_OPTIONS_NAME', 'ad_breadcrumbs_options_name' );
}